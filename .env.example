# Sunucu Ayarları
PORT=8082
NODE_ENV=development

# Database - Neon PostgreSQL (https://neon.tech)
# Neon'dan aldığın connection string'i buraya yapıştır
# Örnek: postgresql://user:pass@ep-xxx-123456.aws.neon.tech/neondb?sslmode=require
DATABASE_URL="postgresql://user:pass@host.neon.tech/neondb?sslmode=require"

# Redis - Docker ile local veya cloud Redis
REDIS_URL="redis://localhost:6379"

# JWT Secrets - GÜVENLİ random stringler kullan! (min 256 karakter)
# PowerShell ile oluştur: -join ((65..90) + (97..122) | Get-Random -Count 64 | ForEach-Object {[char]$_})
JWT_ADMIN_SECRET="change-this-to-super-secure-random-string-min-256-characters"
JWT_TEAM_SECRET="change-this-to-another-different-secure-random-string-min-256-characters"

# JWT Token Süreleri
JWT_EXP_ADMIN="15m"
JWT_EXP_TEAM="60m"

# CORS - Frontend URL'leri (virgülle ayır)
ALLOWED_ORIGINS="http://localhost:3000,http://localhost:3001,http://localhost:5173"